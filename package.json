{
	"private": true,
	"scripts": {
		"build": "tsc --build",
		"dev": "tsc --build --watch .",
		"clean": "pnpm run -r --parallel --aggregate-output clean",
		"node.clean": "pnpm run -r --parallel --aggregate-output node.clean",
		"test": "pnpm exec vitest --run",
		"test.dev": "pnpm exec vitest"
	},
	"type": "module",
	"devDependencies": {
		"@types/node": "catalog:",
		"typedoc": "^0.26.10",
		"typescript": "catalog:",
		"vitest": "^2.1.4"
	},
	"version": "0.0.0"
}
